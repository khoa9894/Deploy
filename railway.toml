[build]
builder = "NIXPACKS"
buildCommand = "cd app && npm install --legacy-peer-deps && npm run build"

[deploy]
startCommand = "cd app && node dist/src/main"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
